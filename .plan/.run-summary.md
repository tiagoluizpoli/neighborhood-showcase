# Active Run Summary

Keep this file short. It should only hold durable conclusions worth loading by default in future runs for the current run family.

## Decisions

- The active PRD is `.plan/prds/PRD-v9-panel-shell-layout-and-navigation.md`.
- The active epic is `E-16` / Panel Shell, Layout, and Navigation.
- `T-16-02` is complete: provider list/index routes use container-owned framing, create/edit routes use `centered-form`, and route-level tests now assert variant selection plus the absence of route-owned `px-6` / `py-8` / `mx-auto max-w-4xl` wrappers.
- `T-16-03` is complete: the sidebar collapse regression was caused by `apps/web/src/routes/panel.tsx` passing `onOpenChange` to `SidebarProvider` without also feeding back the controlled `open` prop. The fix is `open={sidebarOpen}` plus the existing `sidebar:state` localStorage persistence.
- `T-16-04/ST-01` derives sidebar header context from the active panel route and, on moderation routes, the selected condo from `mod_ctx__cndo`; the header now shows brand + section/context and an account utility affordance without hardcoded new copy.
- `T-16-04/ST-02` keeps the top-bar chrome local to `apps/web/src/routes/panel.tsx` by deriving an eyebrow/title/context row from the existing active sidebar labels and moderation condo context; no new top-bar strings or route metadata plumbing were introduced.
- `T-16-04/ST-03` covers the strengthened shell chrome at the panel layout seam in `apps/web/src/routes/-panel.test.tsx`, asserting provider section context plus moderation condo context without widening into deep visual-detail assertions.
- The sidebar regression seam is now covered by Playwright in `apps/web/tests/account.spec.ts`, including expanded/collapsed visual snapshots and persisted collapse state across reload.

## Known Blockers

- `bun run test` is still red outside this task because Bun is trying to execute Playwright specs directly and several unrelated component/unit suites already fail in the baseline. Use targeted verification or `bun run test:e2e` for Playwright until the global test entrypoint is split/fixed.

## Completed Areas

- `T-16-01` Canonical content container primitive.
- `T-16-02` Provider route migration to container variants.
- `T-16-03` Sidebar collapse regression fix.
- `T-16-04` Strengthened sidebar header and top-bar chrome.
