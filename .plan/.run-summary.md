# Active Run Summary

Keep this file short. It should only hold durable conclusions worth loading by default in future runs for the current run family.

## Decisions

- Ralph Loop canonical workflow surface is now `.plan/`, not `.specify/memory/`.
- The active PRD is `.plan/prds/PRD-v8-role-access-and-route-architecture.md`.
- The active epic is `E-14` / Role Access and Route Architecture.
- `T-14-01` is in progress; `ST-01` and `ST-02` are complete, `ST-03` is next.
- The canonical backend-derived Provider-enabled contract now lives at `apps/server/src/application/use-cases/user/get-user-access-profile.ts` and is exposed at the highest touched presentation seam as `user.getAccessProfile`.
- Provider-enabled currently means exactly: `assignmentRepo.hasApprovedResidentAssignment(userId)`.

## Known Blockers

- Historical `.specify/memory` epics beyond epic 13 have not been backfilled into native Ralph Loop task format yet.

## Completed Areas

- PRD history migrated into `.plan/prds/` and `.plan/PRD.md`.
- Current grilling session and handoffs migrated into `.plan/grilling/` and `.plan/handoffs/`.
- Project engineering rules mirrored into `.plan/RULES.md`.
- Project glossary/domain context mirrored into `.plan/CONTEXT.md`.
- T-13 Provider Section Reorg is complete.
