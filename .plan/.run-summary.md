# Active Run Summary

Keep this file short. It should only hold durable conclusions worth loading by default in future runs for the current run family.

## Decisions

- The active PRD is `.plan/prds/PRD-v9-panel-shell-layout-and-navigation.md`.
- The active epic is `E-16` / Panel Shell, Layout, and Navigation.
- `T-16-02` is complete: provider list/index routes use container-owned framing, create/edit routes use `centered-form`, and route-level tests now assert variant selection plus the absence of route-owned `px-6` / `py-8` / `mx-auto max-w-4xl` wrappers.
- The provider route seam tests require a shared `@tanstack/react-router` mock shape across `apps/web/src/routes/-panel.provider.test.tsx` and `apps/web/src/routes/-panel.provider.announcements.test.tsx`; the layout test now exports `Link`, `useNavigate`, `useParams`, `useSearch`, and `useRouteContext` so both suites can run together under Bun.

## Known Blockers

- `bun run test` is still red outside this task because Bun is trying to execute Playwright specs directly and several unrelated component/unit suites already fail in the baseline. Use targeted verification or `bun run test:e2e` for Playwright until the global test entrypoint is split/fixed.

## Completed Areas

- `T-16-01` Canonical content container primitive.
- `T-16-02` Provider route migration to container variants.
