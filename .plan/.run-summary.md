# Active Run Summary

Keep this file short. It should only hold durable conclusions worth loading by default in future runs for the current run family.

## Decisions

- Active PRD is `.plan/prds/PRD-v13-provider-entity-and-verified-stamp.md`; active epic E-20 (Provider Entity Refactor, load-bearing) then E-21 (verified stamp).
- `T-20-01/ST-01` is complete: `provider` is now a first-class table in `packages/db/src/schema/showcase.ts` (`id`, `ownerId → user.id` cascade, `createdAt`/`updatedAt`, soft-delete `deletedAt`) with `providerRelations`. `providerProfile.providerId`, `providerAssignment.providerId`, `announcement.providerId` re-keyed from `user.id` → `provider.id` (relations re-pointed). NO `(ownerId, condominiumId)` unique constraint. ADDITIVE migration `0003_nasty_pyro.sql` (base `0000` untouched); applied to dev+test DBs (the 3 re-keyed tables were TRUNCATEd CASCADE — legacy rows held user-ids; ST-04 seed rebuild repopulates).
- `T-20-01/ST-02` is complete: `Provider` domain entity (`apps/server/src/domain/entities/provider.entity.ts`, extends `AuditableEntity` — getters `ownerId`/`deletedAt`/`isDeleted`, ownership helper `isOwnedBy(userId)`, plus `ProviderNotFoundError`), `ProviderRepository` interface (`create`, `findById`/`listByOwner` excluding soft-deleted, `softDelete`; `CreateProviderInput` param object), `DrizzleProviderRepository` + `ProviderMapper` in infrastructure (soft-delete `isNull(deletedAt)` filters; `crypto.randomUUID()` on create when no id).
- `T-20-01/ST-03` is complete: announcement read paths that still assumed `announcement.providerId === user.id` now thread through `provider.id` first, then resolve the owner `user` record only for user-facing fallback fields. `list-public-announcements.ts`, `find-public-announcement-by-id.ts`, and `announcement-repository/moderation.ts` now join `announcement.providerId -> provider.id -> provider.ownerId -> user.id`; `provider_profile` joins also key on `provider.id` instead of `user.id`. Result: build-green seam for the provider PK without changing surface behavior. Gates: `bun run --filter server check-types` clean; root `bun run check` clean; root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue. Exploratory integration tests show the expected next gap: legacy fixtures still seed `provider_profile` / `provider_location` with old user IDs and need the provider-row rebuild in later T-20-01 work. Next: ST-04 (seed rebuild).
- `T-20-01/ST-04` is complete: `apps/server/src/infrastructure/db/seed.ts` now seeds first-class `provider` rows before assignments / profiles / announcements, explicitly clears `provider_profile` + `provider` on reset, and preserves existing test-facing provider IDs where Playwright relies on them. Added `multi.provider.owner@test.com` with 2 APPROVED RESIDENT providers across 2 condos to satisfy the PRD multi-provider acceptance state; `branding@test.com` remains the single-provider verified state; `provider.transition@test.com` now has a provider row + profile with no approved assignment. Verification: `bun run src/infrastructure/db/seed.ts` succeeded; live query confirmed the 2-provider, single-provider, and no-approved states; `bun run --filter server check-types` and root `bun run check` are clean; root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue. Next: T-20-02 / ST-01.
- `T-20-02/ST-01` is complete: provider-profile read paths now key on `provider.id` and exclude soft-deleted providers. `apps/server/src/infrastructure/db/provider-profile-repository.ts` inner-joins `provider` so `findByProviderId()` only returns profiles for active providers; `apps/server/src/infrastructure/db/user-repository.ts` public-provider lookup now starts from `provider`, joins `provider.ownerId -> user.id` for fallback auth-user fields, resolves `provider_profile` by `provider.id`, and filters soft-deleted providers. Verification: `bun run --filter server check-types` clean; root `bun run check` clean; root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue. Next: T-20-02 / ST-02.
- `T-20-02/ST-02` is complete: `apps/server/src/presentation/routers/provider-profile.ts` now accepts an optional `providerId` on both `get` and `update`, feeds that provider PK into the use-cases, and returns `providerId` in the `get` payload so the router contract is explicitly keyed on `provider.id`. The legacy no-input panel callers still fall back to `ctx.session.user.id`, preserving the single-provider route surface until T-20-05 moves panel URLs onto `$providerId`. Verification: `bun run --filter server check-types` clean; root `bun run check` clean; root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue. Next: T-20-02 / ST-03.
- `T-20-02/ST-03` is complete: provider-profile integration coverage now seeds distinct `provider.id` and `provider.ownerId` values so the suite proves reads/writes no longer assume `provider.id === user.id`. `get-provider-profile.integration.test.ts` and `get-public-provider-profile.integration.test.ts` now assert soft-deleted providers return no profile on panel/public reads; `update-provider-profile.integration.test.ts` now verifies writes persist against the provider PK. Verification: targeted Bun tests for all 3 provider-profile integration files pass; `bun run --filter server check-types` clean; root `bun run check` clean; root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue. Next: T-20-03 / ST-01.
- `T-20-03/ST-01` is complete: public announcement reads now exclude soft-deleted providers while staying keyed on `provider.id`. `apps/server/src/infrastructure/db/announcement-repository/public/list-public-announcements.ts` adds `provider.deletedAt IS NULL` to the public feed filters, and `find-public-announcement-by-id.ts` now resolves the announcement through an inner join on `provider` plus the same soft-delete guard before loading provider-owner fallback data. Verification: `bun run --filter server check-types` clean; root `bun run check` clean (pre-existing Biome deprecation warning + broken symlink info only); root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue. Next: T-20-03 / ST-02.
- `T-20-03/ST-02` is complete: panel/dashboard/moderation announcement paths re-keyed on `provider.id` with provider soft-delete exclusion. `update-announcement.ts` ownership now keys on `input.providerId` (`announcement.providerId === providerId`) instead of the broken `actorId` (user-id) comparison left over from the re-key. `presentation/routers/announcement/provider.ts` `create`/`getDashboardData`/`update` accept an optional `providerId` feeding the active provider PK into the use-cases, falling back to `ctx.session.user.id` for legacy single-provider callers (their `provider.id === user.id` in the seed) until T-20-05 wires panel URLs onto `$providerId` — same seam as T-20-02. Infra soft-delete exclusion (provider inner-joined, `provider.deletedAt IS NULL`) added to `announcement-repository/provider.ts` (`findDashboardAnnouncementsByProviderId`, `findActiveAnnouncementsByProviderId`) and `announcement-repository/moderation.ts` (`listAnnouncementsForModeration` + `listReportedAnnouncements`). `create-announcement` + `list-announcements-for-moderation` use-cases unchanged (create already keys on `input.providerId`; moderation exclusion lives in the repo). Gates: `bun run --filter server check-types` clean; root `bun run check` clean.
- `T-20-03/ST-03` is complete: announcement integration tests rebuilt around distinct `userId`/`providerId` fixtures (provider row inserted before profile/announcements). All three test files (`list-public-announcements`, `get-public-announcement`, `get-provider-dashboard-data`) now seed a separate `user` (userId) and `provider` row (providerId, ownerId=userId), fixing the T-20-01 provider-row FK violations that were blocking these tests. Soft-delete exclusion assertions added to all three: public list returns empty, public detail throws `AnnouncementNotFoundError`, dashboard returns zero announcements + zero stats. CTA assertions in `get-public-announcement` also updated to include `label: null` (field added in E-18 CTA work; test assertions were stale). Gates: 12/12, 8/8, 3/3 per-file. `bun run --filter server check-types` clean; root `bun run check` clean. T-20-03 fully done. Next: T-20-04 (Layered auth — global role + provider-scoped gating).
- `T-20-04` is complete (all 3 STs done): provider-scoped authorization guard (`assertProviderOwnership` / `assertProviderApprovedStanding`) added in `presentation/trpc.ts`, wired at module scope through DI factory `main/di/auth-guard.ts`. Pinned action split: platform-admin actions → `adminProcedure`; condo-moderator → `checkModerator`; provider-scoped ownership-only → `assertProviderOwnership`; ownership+APPROVED → `assertProviderApprovedStanding`; self-scoped → session.user.id (deferred to T-20-05). Admin test updated: added provider rows for target users in `beforeAll` (FK: `providerAssignment.providerId → provider.id`, `toggleProviderVisibility` upserts `provider_profile`); non-admin rejection tests now assert `{ code: 'FORBIDDEN' }`. Provider-profile integration test rebuilt with distinct userId/providerId fixtures; covers ownership-denied (FORBIDDEN), no-APPROVED-standing denied via `announcement.create` (FORBIDDEN), and owner+APPROVED-standing allowed (create resolves with correct providerId). 14/14 + 8/8 per-file. Next: T-20-05.
- `T-20-05/ST-01` is complete: panel `$providerId` route segment + legacy redirects + client ownership gate. Active provider is URL-encoded with NO session/localStorage store — the value is derived from the `$providerId` param every render, so switching is just navigation and routes are refresh-safe + deep-linkable. `panel.provider.$providerId.tsx` layout renders `ActiveProviderIdProvider` (`panel/provider/-active-provider-context.tsx`: provider + `useActiveProviderId()` hook, throws outside the provider) and a `beforeLoad` ownership gate calling `providerProfile.get({ providerId })` (resolves only owned providers, throws otherwise) → on throw redirects to the caller's default provider via `requireDefaultProviderId` (`panel/provider/-resolve-active-provider.ts`: `resolveDefaultProviderId` no-input + `requireDefaultProviderId` → `condo-setup` when the caller owns none). This complements the authoritative T-20-04 server guards so a deep-link to an unowned provider never renders a broken page. Full route content was moved off the legacy paths into the `$providerId` variants (`$providerId/` index, `$providerId/configuration`, `$providerId/announcements` + `/`,`new`,`$id`,`$id/edit`, `anuncios/$id/pagamento`) with every internal `Link`/`navigate` threading `params.providerId`; `-announcement-form.tsx` now reads `useActiveProviderId()` for `providerProfile.get`/`getDashboardData` queries, `create`/`update` mutations, and navigation. Legacy segment-less routes (`panel.provider.index`, `panel/provider/configuration`, `panel.provider.announcements.{index,new,$id.index,$id.edit}`, `panel.provider.anuncios.$id.pagamento`) became `beforeLoad`-only redirects into their `$providerId` variants (forwarding `id`/`message`); `condo-setup` left untouched for ST-04. Known limitation: a multi-provider caller with no usable default lands in `condo-setup` until the My Providers page (ST-02) becomes the landing — no redirect loop. Tests self-corrected after the legacy→redirect move + the form's new context dep: `-panel.provider.announcements.test.tsx` 19/19 (imports repointed to `$providerId`, `Route.useParams` stubs carry `providerId`, `renderRoute` wraps `ActiveProviderIdProvider`), `-provider-profile.test.tsx` 20/20 (config render repointed + wrapped), `-panel.test.tsx` 8/8 unaffected. Gates: `bun run --filter web check-types` clean except the pre-existing web TS5103 `--ignoreDeprecations` error (untouched); `bun run check` clean (pre-existing optional-chain warning + broken-symlink info only). Next: T-20-05 / ST-02 (My Providers page).
- `T-20-05/ST-02` is complete: My Providers page + the owner-facing list contract. T-20-01's `ProviderRepository.listByOwner` existed but was unsurfaced to the client; ST-02 wired it through (escalate-if was about the repo capability, which is present — no escalation). Server: `application/use-cases/provider/list-owned-providers.ts` `ListOwnedProviders` (ctor-injected `ProviderRepository` + `ProviderProfileRepository`; `execute({ ownerId })` → `listByOwner` [soft-deleted excluded] enriched with `displayName`/`logoUrl` from each provider's profile, since the `provider` entity carries no human label → `OwnedProviderSummary[]` `{ id, displayName, logoUrl }`). `provider-profile.ts` router: `listMine` query (`protectedProcedure`, owner-scoped on `ctx.session.user.id`, no per-provider ownership guard since it only ever returns the caller's own). `main/di/provider-profile-router.ts`: deps extended with `listOwnedProvidersUseCase`, wired with `DrizzleProviderRepository` + `ProviderProfileRepositoryImpl`; the real `appRouter` picks it up so `provider-profile.integration.test` (`appRouter.createCaller`) is unaffected. Web: `panel.provider.my-providers.tsx` (route `/panel/provider/my-providers`, a sibling of `$providerId` under the `panel.provider` group layout — OUTSIDE the ownership-gated `$providerId` layout, so reachable with zero providers). `useQuery(trpc.providerProfile.listMine.queryOptions())`. Zero → empty state (`my-providers-empty`) with a CTA into `/panel/provider/condo-setup` ("create your first provider"); populated → card grid, each card a `Link to="/panel/provider/$providerId"` threading `params.providerId`; unnamed providers fall back to `my_providers.unnamed`. `routeTree.gen.ts` regenerated via `vite build` (no `tsr` CLI in repo). i18n `my_providers.*` added pt+en. Tests: `-panel.provider.my-providers.test.tsx` 2/2 (empty CTA `data-to=/panel/provider/condo-setup`; populated cards `data-to=/panel/provider/$providerId` + `data-params={providerId}`; unnamed renders the localized placeholder); sibling web suites unaffected (`-panel.provider.test.tsx` 2/2, `-panel.provider.announcements.test.tsx` 19/19, `-provider-profile.test.tsx` 20/20). Known limitation from ST-01: legacy redirects + `requireDefaultProviderId` still send a no-default caller to `condo-setup`, not this page; making My Providers the multi-provider landing folds into ST-03. Gates: server check-types clean; web check-types clean except the pre-existing TS5103 `--ignoreDeprecations`; `bun run check` clean (pre-existing optional-chain warning + broken-symlink info only). Next: T-20-05 / ST-03 (persistent header provider switcher).
- `T-20-05/ST-03` is complete: persistent header provider switcher mounted once in the shared panel shell. `apps/web/src/routes/panel/provider/-provider-switcher.tsx`: `ProviderSwitcher({ activeProviderId })` reads `providerProfile.listMine` (owner-scoped, from ST-02); renders nothing when the caller owns no providers (inert for non-provider users + the zero-provider state, which the My Providers page handles). A base-ui `Popover` whose trigger label reflects the URL-derived active provider (the `$providerId` segment — NOT a store), falling back to `provider_switcher.select` when none is active. Extracted presentational `ProviderSwitcherItems({ providers, activeProviderId })` (testable without opening the portal): one `Link to="/panel/provider/$providerId"` per owned provider (selecting = navigation, no client state), the active one marked `data-active` + a check, unnamed → `my_providers.unnamed`, plus a `Link` into `/panel/provider/my-providers`. Shell wiring (`panel.tsx`): `PanelLayout` reads `useParams({ strict: false })` → `activeProviderId = params.providerId ?? null` (null outside a `$providerId` context, e.g. dashboard/moderation/admin), and places `<ProviderSwitcher>` in the persistent top header's right control group, before the theme/language controls, so it shows across every panel section. i18n `provider_switcher.*` added pt+en (label/aria/select/manage). Tests: `-provider-switcher.test.tsx` 4/4 (items nav `data-to`/`data-params` + active marker + unnamed fallback + manage link; switcher null on zero providers, trigger reflects the active provider, select placeholder when none active); sibling web suites unaffected (`-panel.provider.my-providers.test.tsx` 2/2, `-panel.provider.test.tsx` 2/2, `-panel.provider.announcements.test.tsx` 19/19, `-provider-profile.test.tsx` 20/20). Gates: web check-types clean except the pre-existing TS5103 `--ignoreDeprecations`; `bun run check` clean (pre-existing optional-chain warning + broken-symlink info only); dependency-cruiser no layer violations. Known limitation (from ST-01/ST-02): My Providers is not yet the multi-provider landing — folds into ST-04's create-flow landing wiring. Next: T-20-05 / ST-04 (repeatable `condo-setup` create-provider flow).
- `T-20-05/ST-04` is complete → **E-20 fully done**. Repeatable condo-setup create-provider flow + the backend create-provider contract. Escalate-if NOT triggered (precedent ST-02: T-20-01 delivered `ProviderRepository.create`, so wiring it through application+presentation is in-scope). Key finding: before this, NO runtime path minted a `provider` row — every flow hardcoded `providerId = ctx.session.user.id`, working only because the seed keeps `provider.id === user.id` for single-provider users; a brand-new signup owns no provider row, so the first AND the Nth provider both need minting. Backend: `application/use-cases/provider/create-provider.ts` `CreateProvider` (ctor `ProviderRepository`+`ProviderProfileRepository`; `execute({ownerId,displayName})` mints via `providerRepo.create` AND upserts a minimal **hidden** default profile `isProviderVisible:false` — load-bearing because every `$providerId` panel read goes through `providerProfile.get` which throws NOT_FOUND for a profile-less provider, so the ownership gate would otherwise bounce a fresh provider; returns `{providerId}`). `provider-profile.ts` router `create` mutation (protectedProcedure, owner-scoped, zod displayName min3/max100). `main/di/provider-profile-router.ts` wires `createProviderUseCase`. `assignment.ts` `request`+`registerExternal` accept optional `providerId`; when supplied `assertProviderOwnership` enforces ownership (closes a self-scoped hole); omitted → legacy `session.user.id`. `RequestAssignment` dup guard keys on `(providerId,condominiumId)` so a new provider can join the same condo as an existing one. Web: `condo-setup.tsx` adds `providerProfile.listMine`; legacy per-user status panels gated behind `ownsProviders===false` (first-time pre-provider state only — e.g. a pending sindico condo request, which mints no provider); owners always get the create selector; resident/external flows navigate to `/panel/provider/$providerId` on success. resident-flow mints (displayName=condo name) then RESIDENT assignment; external-flow mints (displayName from address) then `registerExternal`; sindico-flow unchanged (creates a CONDO; provider materializes on approval). No new visible i18n strings. Tests: `provider-profile.integration.test.ts` 12/12 (added create-mints-owned-provider-with-hidden-default-profile, create-rejects-<3, created-provider-can-request-RESIDENT, request-with-unowned-providerId-FORBIDDEN); `-panel.provider.condo-setup.test.tsx` 2/2; sibling web suites 27/27. Deferred to backlog (all T-20-05/ST-04): condo-setup flow-file i18n sweep; reword selector heading for create mode; surface sindico condo-request pending status for multi-provider owners; make My Providers the no-default landing instead of condo-setup. Gates: server check-types clean; web check-types clean except pre-existing TS5103; `bun run check` clean (pre-existing optional-chain warning + broken-symlink info only); dependency-cruiser no violations. Next: **T-21-01 / ST-01** (get-public-profile condo contract, E-21 verified resident stamp).

- The active epic is `E-17` / Announcement Creation and Authoring Model (finishing) + `E-18` / Announcement Detail, Edit Split, and Analytics (next).
- `T-17-02` is complete: announcement create now uses the canonical structured contact contract (`contact.mode` + optional `contact.custom`) with inherited baseline/default-call semantics and Playwright coverage.
- `T-17-03` is complete: provider announcement detail/edit now matches create for inherited/custom contact authoring, inherited announcements resolve live provider defaults on read, customized announcements stay isolated, and detail views explain the mode clearly on both provider and dashboard alias routes.
- `T-17-04` is complete: bounded CTA model (`apps/server/src/domain/entities/cta.ts`) is first-class and separate from contact — 1 primary + ≤3 secondary over {provider_profile, website, instagram, tiktok, whatsapp}, persisted in a new `announcement.cta` jsonb column, authored in create/edit, and rendered on public card/detail. Public DTOs are sanitized server-side (`sanitizeCta`) so absent/invalid CTA falls back to WhatsApp/call instead of a dead link.
- `T-17-06/ST-01` is complete: seed.ts extended with `authoring@test.com` provider (primaryPhone: +5511966667777, callEnabled: true) plus 4 authoring-matrix announcements: `seed-announcement-auth-inherit` (inherit mode), `seed-announcement-auth-custom` (custom, callEnabled: false), `seed-announcement-auth-cta-present` (inherit, provider_profile CTA), `seed-announcement-auth-cta-fallback` (inherit, website/null CTA → sanitizes server-side). Does not affect providerUser tab counts or any existing visual snapshots.
- `T-17-06/ST-02` is complete: Playwright coverage added across 3 spec files. configuracoes.spec.ts: contact defaults block for authoring@test.com (primaryPhone field assertion + save/restore cycle). meus-anuncios.spec.ts: 4-test authoring matrix block covering inherit/custom mode badges and CTA editor types. public-provider.spec.ts: CTA matrix block asserting provider_profile CTA href and WhatsApp fallback when CTA sanitized to null. 39 tests all pass.
- `T-17-06/ST-03` is complete: Visual regression screenshots added to all 4 authoring-matrix tests in meus-anuncios.spec.ts — auth-inherit-detail, auth-custom-detail, auth-cta-present-edit, auth-cta-fallback-edit. Baselines generated in meus-anuncios.spec.ts-snapshots/. 13/13 tests pass. E-17 is fully complete.
- `E-17` Announcement Creation and Authoring Model — ALL tasks done.
- Active epic: E-18. Task T-18-02 in-progress; next sub-task T-18-02/ST-03.
- `T-18-01/ST-01` is complete: the all-inline create form was lifted out of `panel.provider.announcements.new.tsx` into a new `apps/web/src/routes/panel/provider/-announcement-form.tsx` exporting `AnnouncementForm`. All authoring sections stay composed unchanged; create route is now a 10-line thin wrapper. Edit wiring is T-18-02.
- `T-18-01/ST-02` is complete: per-field lockability seam added to `AnnouncementForm`. Exported `AnnouncementFieldKey` / `AnnouncementFieldPolicy` ({ editable }) / `AnnouncementFieldPolicyMap`, plus `resolveAnnouncementFieldPolicy(mode)` whose default map locks identity (`id`) and keeps every other field (incl. `category`) editable for create+edit (MVP). `AnnouncementForm` takes an optional `fieldPolicy` override and resolves `policy = fieldPolicy ?? resolveAnnouncementFieldPolicy(mode)`. `disabled={!policy.<field>.editable}` is wired on native controls (location Select, title/subtitle Input, description Textarea, image buttons, verified Checkbox). Composed sections (category/price/tags/contact/cta) carry policy entries but aren't disabled-wired (no `disabled` prop yet; out-of-scope to change them) — freezing one later adds a single `disabled` prop to that one component. Default = all editable except non-rendered `id`, so create behavior is unchanged.
- `T-18-01/ST-03` is complete: confirmed create route is a clean 10-line thin wrapper `<AnnouncementForm mode="create" />` with no dead inline code (ST-01 already completed the migration). Gates: `bun run check-types` clean, `bun run check` clean (pre-existing biome-config warning + broken symlink only). T-18-01 FULLY DONE. T-18-02 is now unblocked.
- `T-18-01` Extract shared AnnouncementForm and field-policy seam — DONE. Next: T-18-02 Edit route split onto shared form and delete duplicates.
- `T-18-02/ST-01` is complete: edit mode is wired into the shared `AnnouncementForm` and a dedicated edit route exists at `apps/web/src/routes/panel.provider.announcements.$id.edit.tsx` (`<AnnouncementForm mode="edit" announcementId={id} />`; route `/panel/provider/announcements/$id/edit`). Edit fetches via `announcement.getDashboardData` (`enabled: isEditMode`), flattens+finds by id, prefills every field once (`prefilledRef` guard), and submits via `announcement.update` carrying `id` (no `providerAssignmentId` — location is fixed on update); create still submits via `announcement.create`. New `existingImageUrl` state shows the saved cover with a Change button (no forced re-crop); submit reuses the existing URL unless a new image is cropped. Mode-aware copy via new keys `new_announcement.edit_title/edit_subtitle/submit.update/submit.updating` (en+pt); update toasts reuse `meus_anuncios.detail.update_success/update_error`. NOT yet done (ST-02/ST-03): detail-page Edit button still routes inline-edit, and `panel.dashboard.announcements.new.tsx` + `-provider-dashboard-edit-form-fields.tsx` are not yet deleted. Gates: check-types clean (4/4); check clean (pre-existing biome-config warning + broken symlink only); `-panel.provider.announcements.test.tsx` 6/6 pass (create unchanged).
- `T-18-02/ST-02` is complete: identity-lock policy formalized and exercised. `resolveAnnouncementFieldPolicy` now uses `mode` (was `_mode`); `id` policy is mode-conditional: `LOCKED_FIELD` in edit, `EDITABLE_FIELD` in create (id doesn't exist as an authorable concept in create). All other fields remain `EDITABLE_FIELD` for MVP. The `id` field is not rendered in the form UI so behavior is unchanged — this change makes the seam meaningful and removes the placeholder `_mode`. Gates: check-types clean (4/4); check clean (pre-existing biome-config warning + broken symlink only). T-18-02 DONE.
- `T-18-03` is complete (ST-01 + ST-02 done in single iteration): `panel.provider.announcements.$id.tsx` rebuilt as facts-first read-only detail. `AnnouncementPresentationPrimitive` (full-width 4:3 hero) removed; new primary block uses `flex-col lg:flex-row` — left flex-1 holds status badge + title/subtitle + `DetailList` (category/price/condo/dates) + `AnnouncementContactCard`; right side holds constrained 4:3 image cover (`lg:w-[300px]`, `aspect-[4/3]`, `object-cover`, no re-crop). Summary mini-card (status/contact-count/tags) removed along with `SummaryRow` and `countContactLinks` helpers. Secondary block (description/tags/suspension) below the primary block. Analytics panel stays full-width below (resize is T-18-04). Edit button navigates to `$id/edit`. No inline-edit state. All strings via existing i18next keys. Gates: check-types 4/4 clean; check clean.
- `T-18-04` is complete (ST-01 done): analytics block was already below the facts block after T-18-03. Changed `h-[320px]` → `h-[210px]` in all three positions in `-provider-dashboard-analytics-panel.tsx` (loading state div, error state div, chart container div). Metric cards (`grid gap-4 md:grid-cols-3`) render unconditionally before the chart — always visible. Detail page had no changes needed. Gates: check-types 4/4 clean; check clean (pre-existing biome-config warning + broken symlink only). T-18-04 FULLY DONE. Next: T-18-05 (test matrix and boundary guards).
- `T-18-05/ST-01` is complete: shared-form parity + field-policy lock tests in `-panel.provider.announcements.test.tsx`. Replaced two stale `$id` inline-edit tests (obsolete after T-18-02/T-18-03 read-only split) with edit-route tests rendering `<AnnouncementForm mode="edit">`. Covers edit prefill, edit submit → `announcement.update` carrying id (no `providerAssignmentId`, never create), create submit → `announcement.create` (with `providerAssignmentId`, no id, never update), same-input-surface across modes, and field-policy lock (id non-editable in edit, no `#id` input, frozen title via `fieldPolicy` prop renders disabled). Seam: trpc mock `mutationFn` records `{method,variables}` into `mutationCalls`. happy-dom: category combobox stubbed at test boundary (no other suite imports it → leak-safe); `react-easy-crop` stub fires `onCropComplete`; `@/utils/crop-image` is NOT module-mocked (would red `crop-image.test.ts`) — create test patches `Image`/`canvas`/`fetch` locally and restores in `finally`. Gates: check-types 4/4 clean; check clean. 11/11 per-file; 12/12 co-run with `crop-image.test.ts` (no leak).
- `T-18-05/ST-02` is complete: 5 view-page facts-first + analytics assertions added to `-panel.provider.announcements.test.tsx`. (1) h1 title precedes analytics `<section>` in document order (compareDocumentPosition bit 4). (2) Cover image has `object-cover` class, `aspect-[4/3]` wrapper, `lg:w-[300px]` constraint (not full-width hero). (3) No `border-dashed` element in output (removed summary mini-card; analytics chart path renders since `getAnalytics` has `initialData:null` → not loading, not error). (4) Metric card labels "Visualizações"/"Interações"/"Conversão" all present (unconditional render). (5) `h-[210px]` in innerHTML, `h-[320px]` absent. Added `importDetailRoute` helper. Gates: check-types 4/4 clean; check clean. 16/16 per-file; 17/17 co-run with `crop-image.test.ts` (no leak).
- `T-18-05/ST-03` is complete: 2 tests added to `-panel.provider.announcements.test.tsx`. (1) Create regression guard: `<AnnouncementForm mode="create" />` still has `input[type="file"]`, `[data-testid="contact-mode-inherit-badge"]`, and `[data-testid="cta-section"]` after shared-form extraction. (2) Public boundary guard: `_portal.anuncios.$id` with a full `mockPublicAnnouncementData` fixture has no metric card labels (Visualizações/Interações/Conversão), no analytics height classes, no `/edit` href, no "Editar" text. Inlined into existing file (reuses established trpc/auth-client mocks — no new `mock.module` calls, zero leakage risk). `importPortalRoute` helper added. `mockPublicAnnouncementData` module variable added to trpc mock. Gates: check-types 4/4 clean; check clean. 18/18 per-file; 19/19 co-run with `crop-image.test.ts` (no leak). T-18-05 in-progress; next ST-04 (Playwright create/edit end-to-end with seeded data).

- `T-19-01/ST-01` is complete: created `apps/web/src/utils/provider-identity.ts` with `resolveProviderIdentity(ProviderIdentityInput): ProviderIdentityResult`. `ProviderIdentityMark` discriminated union — `{ kind: 'logo'|'avatar'; src }` or `{ kind: 'initials'; initials }`. `bannerUrl` is strictly separate (passthrough field in result, never an identity mark). `deriveInitials` matches existing `getInitials` in the public page (`_portal.providers.$id.tsx`). All exports named. Gates: biome check clean (pre-existing warning/info only); tsc clean on provider-identity.ts. Next: ST-02 (unit tests).
- `T-19-02/ST-01` is complete: additive original-image retention schema. Added 3 nullable columns to `provider_profile` in `packages/db/src/schema/showcase.ts` — `avatarOriginalUrl`/`logoOriginalUrl`/`bannerOriginalUrl` (`*_original_url`), each beside its cropped `*_url`. Contract: `*_url` = derived crop (unchanged semantics); `*_original_url` = untouched full-res upload, the re-crop source T-19-03 depends on. `bun run db:generate` produced ADDITIVE migration `0001_outgoing_ozymandias.sql` (3× `ALTER TABLE ADD COLUMN text`) + `meta/0001_snapshot.json`; journal idx 1 appended; base `0000_concerned_violations.sql` UNCHANGED (postgis/seed SQL intact — no rebuild, no escalation). Applied directly to dev + test DBs via `docker exec ... psql` `ADD COLUMN IF NOT EXISTS` (db:push blocks on postgis); verified present in both. Gates: check-types db-package clean (only pre-existing web TS5103); check clean. Next: ST-02 (thread original-source through entity/repo/mapper/DTOs + get/update + public-profile use-cases).
- Active epic: E-19, task T-19-02 in-progress, next sub-task ST-02.
- `T-19-03/ST-01` is complete: `ImageUploadField` role-parameterized; raw URL text box removed from provider UI; URL data path (value/onChange) preserved.
- `T-19-03/ST-02` is complete: filled state exposes Replace / Re-crop / Remove. Re-crop reopens the cropper on the ORIGINAL full-res upload (`originalValue` from the T-19-02 contract; falls back to the cropped `value` for legacy rows) with NO new upload; new-file picks (Upload/Replace) also upload the untouched original via `onOriginalChange`; Remove clears both. CONTRACT DIVERGENCE: the shape prop is named `imageRole`, NOT `role` — biome `lint/a11y/useValidAriaRole` rejects `role="avatar"/"logo"` at the call site and RULES §5 forbids biome-config edits; this was the root cause of the prior 9 failed attempts. Component strings now route through `useTranslation('configuracoes')` with new `image_upload_*` keys in en+pt. Call site wires `imageRole` + `originalValue`/`onOriginalChange` to new `*OriginalUrl` state and the save mutation sends the 3 `*OriginalUrl` fields. `profile-preferences.tsx` (account, no `imageRole`) preserved. Gates clean (only pre-existing TS5103 + biome-config warning). Next: T-19-03/ST-03 component test.

- `T-21-01` is complete: the public provider-profile contract now includes `verifiedCondo: { condoId, condoName } | null`, sourced through the existing `providerAssignment -> condominium` join in `DrizzleUserRepository.findPublicProviderById`. `GetPublicProviderProfile` preserves the existing `isVerified` boolean but now exposes the condo payload only for APPROVED RESIDENT providers; EXTERNAL, MODERATOR, PENDING, REJECTED, and no-assignment cases resolve `verifiedCondo` to `null`. Verification: `bun test apps/server/src/application/use-cases/user/get-public-provider-profile.integration.test.ts` 10/10 pass, `bun test apps/server/src/presentation/routers/user-get-public-profile.integration.test.ts` 3/3 pass, `bun run --filter server check-types` pass, `bun run check` pass. Root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` issue; root `bun run test` remains red on pre-existing provider-FK fixture debt outside T-21-01.

- `T-21-02/ST-01` is complete: added shared `apps/web/src/components/verified-resident-stamp.tsx` exporting `VerifiedResidentStamp({ condoName, variant })` so the hero pill and announcement-card mark can share one check-seal + condo-name-only visual primitive. The component localizes the shared tooltip/`aria-label` through `verified_resident_stamp.label` in both locale files (`Verified resident at {{condo}}` / `Morador verificado em {{condo}}`), keeps the icon `aria-hidden`, and exposes a `variant` seam (`hero` / `card`) for the next UI slices. Verification: `bun run check` pass; root `bun run check-types` still fails only on the pre-existing web TS5103 `--ignoreDeprecations` config error, and targeted `bun x tsc --project apps/web/tsconfig.json --ignoreDeprecations 5.0` surfaced only pre-existing condo-setup prop mismatches outside this slice.

## Known Blockers

- `bun run test` is still red outside this task because Bun is trying to execute Playwright specs directly and several unrelated component/unit suites already fail in the baseline. Use targeted verification or `bun run test:e2e` for Playwright until the global test entrypoint is split/fixed.
- `dashboard.spec.ts` slim screenshot and `-provider-profile.test.tsx` (3 cases) fail on the clean tree too — pre-existing, unrelated to E-17.
- `bun --hot` (server dev) does NOT reload `packages/db` schema changes; after adding a schema column, restart `bun run dev` or the running server 500s on queries projecting the new column. `db:push` also blocks non-interactively on dropping the postgis `spatial_ref_sys` table — apply additive columns with a direct `ALTER TABLE` against both `.env` and `.env.test` DBs.

## Completed Areas

- `T-17-01` Provider contact defaults and WhatsApp baseline. (sync fix: was done in code but task file showed "ready")
- `T-17-02` Create flow inherited contact authoring.
- `T-17-03` Edit flow contact parity and live inheritance.
- `T-17-04` Bounded CTA authoring and public fallback.
- `T-17-05` Structured category, tags, and money primitives.
- `T-17-06/ST-01` Authoring model seed matrix added.
- `T-17-06/ST-02` Playwright coverage for provider config, authoring matrix detail views, and public CTA behavior.
- `T-17-06/ST-03` Visual regression screenshots locked for the 4 authoring-matrix tests; 4 new baselines in meus-anuncios.spec.ts-snapshots/.
- `T-17-06` Authoring surface regression and seeded Playwright matrix — DONE.
- `E-17` Announcement Creation and Authoring Model — ALL tasks done.
- `T-20-04/ST-02` Pinned the platform-admin vs provider-scoped vs condo-moderator vs self-scoped action split and applied guards. `admin.ts` → `adminProcedure` (role gate moved into the procedure layer; `checkGlobalAdmin` helper + inline FORBIDDEN throws removed). `provider-profile.ts` `get`/`update` and `announcement/provider.ts` `update`/`getDashboardData` → `assertProviderOwnership`; `announcement/provider.ts` `create` → `assertProviderApprovedStanding`. Closed a real leak: `assignment.pendingCount` was ungated → now `checkModerator`. Self-scoped (request/getMyAssignments/registerExternal, payment/analytics announcement procs) documented and deferred to T-20-05 for `$providerId` threading. `context.ts` again unchanged. No router-level tests hit the newly-guarded create/update/getDashboardData/request paths; `provider-profile.integration.test.ts` (a/b) will fail the ownership guard until ST-03 rebuilds its fixtures (that file is ST-03's). Gates: server check-types clean; root `bun run check` clean (pre-existing optional-chain warning + broken-symlink info only). Next: T-20-04/ST-03 (authorization integration tests).
