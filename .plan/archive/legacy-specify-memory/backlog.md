# Backlog

This file tracks ideas and follow-up work deliberately deferred during planning or grilling sessions. Deferred means "not in the current implementation slice", not "rejected". Items are picked up for implementation when a specific PRD or issue activates them.

Status values: `deferred` | `in-progress` | `completed` | `cancelled`

| Status | Area | Item | Why deferred | Revisit trigger | Linked |
| --- | --- | --- | --- | --- | --- |
| deferred | Categories | Category admin UI (create/edit/deactivate/reorder) | Backend-managed categories needed first; admin CRUD expands scope beyond home-page cleanup. | After category table and seeded category flow are stable. | Issue 62 |
| deferred | Categories | Analytics-backed trending categories (aggregate table/job) | Trending should not scan raw analytics on public home requests; aggregate infrastructure deserves a dedicated slice. | When category filtering is stable and enough analytics data exists to rank categories meaningfully. | Issue 62 |
| deferred | Frontend | Frontend architecture cleanup sweep | Issue 55 focuses on backend Clean Architecture first; frontend concerns need separate discovery and scope. | After backend architecture sweep is underway or complete. | Issue 55 |
| deferred | Announcement | Full visual redesign of `/anuncios/:id` | Issue 60 only removes duplicate modal/source-of-truth behavior; detail visual polish should follow home/card direction later. | After Issues 58, 59, and 60 are implemented. | Issue 60 |
| deferred | Providers | Public provider directory page | Header decision uses `Anunciar` instead of `Prestadores` for MVP because no public directory exists yet. | When provider profile/listing strategy is ready for a public directory. | Issue 56 |
| deferred | Geolocation | Live location tracking with `watchPosition()` | Background refresh after prior grant is enough for MVP; avoids battery/privacy complexity. | If usage shows Visitors need live movement updates during long browsing sessions. | Issue 57 |
| deferred | Home | Public sort dropdown or explicit sorting controls | Relevance-first ranking should stabilize before exposing sort choices that add UI and query complexity. | If users need to choose between newest, nearest, verified, or relevance ordering after MVP ranking is validated. | Issues 57, 58 |
| deferred | Legal | Public legal/support pages (privacy policy, terms, contact) | Footer should not link to dead legal placeholders; product needs these pages before broader launch. | Before public launch or when footer/legal compliance work is prioritized. | — |
| deferred | Provider | Company Provider (CNPJ / legal-entity profile type — Option B) | Today's Provider Profile (Option A) assumes an individual (one User, one CPF). Future scope: `providerType: COMPANY`, CNPJ + razão social + nome fantasia + document upload, separate onboarding step, CNPJ validation, admin verification. **Grilled 2026-06-10 — deferred to a future epic.** | When marketplace needs to invoice or pay out to legal entities, or when an admin must verify company documents. | — |
| deferred | Routing | Mixed-language route naming fix (PT → EN) | **Operational policy** for the agent: when an implementation task touches a PT-named item, it MUST translate that item as part of the same change AND log any other PT-named items it found in the same touched area but didn't translate. **Discovered-and-stacked items are separate rows in this backlog file** (see the three "Stacked 2026-06-10" rows below). Logged items get migrated in a future sweep epic. Already known in this backlog: `/panel/dashboard/anuncios/...` route family, `panel.conta` route file, `anuncios` PT file-naming convention, `dashboard.anuncios.*` i18n key prefix. | Before any future route work to avoid propagating mixed naming. | — |
| completed | Routing | Stacked 2026-06-10 — `panel.conta` route file + URL rename (PT → EN) | Completed in epic 13 task `06_conta_e_seguranca`: route file translated to `panel.account.tsx`, URL translated to `/panel/account`, and callers updated. | Completed 2026-06-10. | Issue 13 task 06 |
| deferred | Routing | Stacked 2026-06-10 — `panel.dashboard.anuncios.*` route file family + URL rename (PT → EN) | Create-route slice completed in epic 13 task `07_meus_anuncios_list`: `/panel/dashboard/anuncios/novo` now redirects to `/panel/dashboard/announcements/new`. Remaining PT-named item in the touched route family is the payment route `/panel/dashboard/anuncios/$id/pagamento` (plus its redirect aliases) which still needs the future EN sweep. Logged per the operational policy. | Future sweep epic (finish `anuncios` → `announcements` for payment routes and aliases). | — |
| deferred | i18n | Stacked 2026-06-10 — `dashboard.anuncios.*` i18n key prefix rename (PT → EN) | i18n key prefix `dashboard.anuncios.*` in `apps/web/src/locales/{en,pt}/translation.json` (PT "anuncios" → EN "announcements"). Used by the announcement create + payment routes. Logged per the operational policy in the "Mixed-language route naming fix" row above. | Future sweep epic (rename `dashboard.anuncios.*` → `dashboard.announcements.*`). | — |
| completed | i18n | Backend language preference persistence (per-user, cross-device) | Completed in epic 13 task `06_conta_e_seguranca`: the Preferences section persists language/theme through `trpc.user.update`, and the header toggles now also do silent best-effort backend writes. | Completed 2026-06-10. | PRD-v7 |
| active | Provider | Provider configuration page (`displayName`, `companyName`, `tradeName`, `avatarUrl`, `logoUrl`, `bannerUrl`, `publicDescription`, social links, `isProviderVisible`) | Provider should have own branding fields, not derived from User. **Scope locked 2026-06-10: full branding set in Option A. Strict User/Provider Profile split. No CNPJ. URL inputs only (no image upload widget — separate UX slice).** **Status update 2026-06-10 (merge into PRD-v7 / epic 13):** scope expanded to include the image upload widget (dual-mode for Provider fields, widget-only for User avatar). Now in scope for epic 13 task `05_configuracoes_page`. The remaining "URL inputs only" caveat is RESCINDED — the upload widget is in scope. | Active. Epic 13. | PRD-v7 |
| active | Provider | Announcement list page ("Meus Anúncios") | Stub route exists but no full list page yet. Provider needs a page to see all their announcements (active/draft/expired/suspended tabs, currently embedded in dashboard). **Locked 2026-06-10: header + tabs + cards lifted from dashboard; component state, no URL state in this pass.** **Status update 2026-06-10 (merge into PRD-v7 / epic 13):** in scope for epic 13 task `07_meus_anuncios_list`. | Active. Epic 13. | PRD-v7 |
| deferred | Admin | Reports section (top-level block, ADMINISTRATOR only) | Sidebar "Reports" block is a placeholder. Full reporting functionality deferred to future slice. | When admin reporting is prioritized. | — |
| deferred | Admin | Admin provider management (global listing, condo filter, user drill-down) | Admin needs to see all providers globally, filter by condo, and drill into provider's user. | When admin provider management is prioritized. | — |
| deferred | Admin | Admin condominium management (approve/reject new condos) | System needs a UI for approving/rejecting new condominium applications. | When condo onboarding flow is built. | — |
| deferred | Frontend | Branding (name, logo, color palette, design system) | "NS" placeholder used for now. Product name, logo, and brand identity deferred until after MVP. | When product identity is defined. | — |
| deferred | Moderation | Live badge counts for sidebar moderation queues | Requires three new tRPC endpoints: `announcement.pendingCount`, `assignment.pendingCount`, `report.openCount`. Stub counts (0) used in PRD-v5. | When moderation queue endpoints are built. | PRD-v5 |