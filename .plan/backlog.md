# Backlog

This file tracks deferred or follow-up work that remains relevant after the Ralph Loop cutover.

Status values: `ready` | `in-progress` | `blocked` | `done`
Horizon values: `now` | `soon` | `later`

| Status | Horizon | Area | Item | Notes | Linked |
| --- | --- | --- | --- | --- | --- |
| ready | now | Migration | Backfill completed historical `.specify/memory` epics into native Ralph Loop task format | Pass A migrated the active PRD lineage and epic 13 only. Older completed epics remain preserved in legacy `.specify/memory/epics/` until backfilled. | `.plan/archive/legacy-specify-memory/` |
| ready | now | Migration | Curate historical grilling material into `.plan/summaries/` | The latest grilling session is canonical in `.plan/grilling/`; the large legacy grilling log was archived and still needs optional curated summaries. | `.plan/archive/legacy-specify-memory/grilling_history.md` |
| ready | later | Provider | Company Provider (CNPJ / legal-entity profile type — Option B) | Deferred by the 2026-06-10 grilling session; current Provider Profile scope remains individual-only. | PRD-v7 |
| ready | later | Routing | Finish mixed-language route naming sweep (PT → EN) | Some PT-named route/i18n surfaces remain outside the migrated active task path and should be cleaned in a future sweep. | legacy backlog |
| ready | later | Admin | Reports section implementation | Sidebar/reporting placeholder work remains deferred. | legacy backlog |
| ready | later | Admin | Admin provider management and condominium management UIs | Deferred admin surfaces remain outside the current provider-section reorg slice. | legacy backlog |
| blocked | now | Provider / E2E | T-13-09/ST-04 Playwright coverage is blocked on sidebar capability gate + footer avatar image implementation | `apps/web/src/routes/panel.tsx` still has unconditional `GROUP_PROVEDOR.condition = true` and renders only `AvatarFallback`, so truthful E2E coverage for provider-only sidebar visibility and avatar image/fallback cannot pass until ST-02 lands or app-code edits are allowed. | `.plan/epics/13-provider-section-reorg/tasks/09-dashboard-slim-and-sidebar.md` |
| ready | later | Routing | Translate the remaining public announcement route surface (`/anuncios/:id`, `_portal.anuncios.$id.tsx`) to English | T-13-10/ST-01 renamed the public provider route to `/providers/:id`; the linked public announcement route remains PT-named but was outside this sub-task's scope. | `.plan/epics/13-provider-section-reorg/tasks/10-public-page-and-adrs.md` |
| ready | later | Routing | Rename PT-named payment route within provider namespace (`/panel/provider/anuncios/$id/pagamento`) to EN (`/panel/provider/announcements/$id/payment`) | T-14-02/ST-02 created the payment route under provider namespace but preserved the PT path name (legacy constraint). Rename pending sweep. | `.plan/epics/14-role-access-and-route-architecture/tasks/02-provider-route-group-and-dashboard-shim.md` |
| ready | soon | Provider | Restore independent-provider activation once backend capability storage supports non-condominium enablement | T-14-05/ST-01 disabled the external-provider CTA on the non-Provider activation surface because the current canonical `providerEnabled` contract only unlocks Provider navigation from approved RESIDENT assignments; the old external flow would loop users back into locked onboarding. | `.plan/epics/14-role-access-and-route-architecture/tasks/05-provider-enablement-surface-sync.md` |
| ready | later | Routing | Remove redundant `useUserAccessProfile` access check in `provider-nav.spec.ts` sidebar button assertions (expects `/panel/dashboard` hrefs) — update E2E test to assert `/panel/provider` hrefs after sidebar update in T-14-02/ST-02 | provider-nav.spec.ts checks for `a[href*="/panel/dashboard"]` but sidebar now links to `/panel/provider/*`; test needs update in ST-04 or dedicated sweep. | `.plan/epics/14-role-access-and-route-architecture/tasks/02-provider-route-group-and-dashboard-shim.md` |
| ready | later | i18n | Rename PT-named sidebar translation keys to English (`sidebar.item.denuncias`, `moradores`, `anuncios`, `meus_anuncios`, `configuracoes`, `condominios`, `visao_geral`, `usuarios`) | Rule 6 requires English-only translation keys, but the whole `sidebar.item.*` namespace still uses PT slugs. Found during the moderation route-split review (T-16-06); a lone English key would break namespace consistency, so deferred to a dedicated namespace sweep across `en`/`pt` JSON + all `i18nKey` consumers in `panel.tsx`. | This refactor (T-16-06 code review) |
| ready | later | i18n | Rename the PT-named top-level i18n namespace `configuracoes` to English (`settings`) | Distinct from the `sidebar.item.*` key sweep: `configuracoes` is a registered namespace in `apps/web/src/i18n.ts` (`resources.{en,pt}.configuracoes`) consumed via `useTranslation('configuracoes')` in the provider configuration sections. Rule 6 wants English namespaces; deferred during E-15 Case 6 QA because it ripples to `i18n.ts`, both translation JSONs, and `apps/web/tests/configuracoes.spec.ts`, and should land with the broader PT→EN namespace sweep for consistency. | E-15 Case 6 QA pass |
| ready | soon | Web / UX | Reconcile global `queryCache.onError` toast with inline error states on moderation pages | `apps/web/src/utils/trpc.ts` shows a global error toast for every failed query, while `panel/moderation/condominium.tsx` (and siblings) also render an inline error block — a 4xx like `NOT_FOUND` surfaces the error twice. Pre-existing, surfaced during the moderation route-split review (T-16-06). Decide one surface (suppress global toast for handled queries, or drop the inline block). | This refactor (T-16-06 code review) |
| ready | later | Architecture | Split announcement create/edit god-files (>300 lines, RULES §5) | `panel.provider.announcements.new.tsx` (629), `panel.provider.announcements.$id.tsx` (633), `panel.dashboard.announcements.$id.tsx` (671), and `create-announcement.integration.test.ts` (582) all exceed the 300-line limit. Pre-existing; surfaced during the T-17-05 code review. The two `$id` routes are near-duplicates and should also be de-duplicated. Risky refactor — own task. | T-17-05 code review |
| ready | later | Architecture | Repo methods use loose primitive params (RULES §5 wants object params) | Every `AnnouncementRepository` method (`findById`, `suspend`, `countPendingByCondo`, new `listTagSuggestions`, …) takes loose primitives instead of parameter objects. Repo-wide convention drift, not specific to one change. | T-17-05 code review |
| ready | later | Database / Architecture | Move announcement tags to a dedicated table and joining table | Currently tags are stored directly in the announcements table (e.g. as a collection/JSON array). Transition to a dedicated `tags` table and a many-to-many joining table (e.g. `announcements_tags`) to simplify querying all existing tags and normalize schema. | User request |
| ready | later | i18n | Rename PT-named top-level i18n namespace `meus_anuncios` to English (`my_announcements`) | Distinct from the `sidebar.item.*` sweep (that row covers `sidebar.item.meus_anuncios`): this is a separate top-level key namespace `meus_anuncios.tabs.*` (e.g. `meus_anuncios.tabs.active` → "Ativos"/"Active"). Rule 6 requires English-only keys; only the resolved value should be localized. Found during the route-frame test migration. Sweep with the broader PT→EN key rename. | T-17 test migration |
| ready | now | Testing | Migrate remaining 18 web unit tests off the shallow-renderer onto happy-dom + RTL | Root `bun run test` is now green (server src + packages, 242/0) and the web harness is in place (`apps/web/bunfig.toml` preload → `apps/web/test-setup.ts` registers happy-dom + RTL cleanup; `test:web` script). `language-switcher.test.tsx` is the migrated reference pattern. Remaining files still use `mock.module('react', …)` which leaks across files in one bun process — `bun run test:web` is 140 pass / 46 fail / 9 err until they are converted. Pattern: drop the React mock + shallow renderer, render real component with RTL `render`/`screen`, keep boundary mocks (i18next/trpc/router/lucide), wrap in QueryClientProvider when the component uses react-query. Files: -analytics, -dashboard-analytics (also has a bun ESM `redirect` import bug from @tanstack/react-router), -detail-page, -geolocation, -header, -home-layout, -moderation, -panel, -panel.provider.announcements, -provider-profile, panel/-provider-dashboard-condo-setup-{external,resident,sindico}-flow, panel/-provider-dashboard-payment-flow, panel/-provider-dashboard-route-{frame,message,surface}. (announcement-card + announcement-authoring-controls still mock react but stay green — convert last.) | T-17-05 code review / test-infra |
| deferred | later | i18n | Localize hardcoded PT strings in the condo-setup flow files | `panel.provider.condo-setup.tsx`, `panel/-provider-dashboard-condo-setup-{resident,external,sindico}-flow.tsx` and `-status-panels.tsx` carry hardcoded PT copy (titles, labels, toasts). T-20-05/ST-04 touched these for the repeatable create flow (logic only — mutation sequencing + navigation) and added no new visible strings, so per RULES §6 the bulk copy localization is stacked for a dedicated sweep. | T-20-05/ST-04 |
| deferred | soon | Web / UX | Reword condo-setup selector heading for repeatable create mode | The selector still reads "Você ainda não possui uma localização cadastrada" — misleading now that condo-setup is the repeatable create-provider flow used for the Nth provider too. Reword to a create-mode neutral heading (and localize per the i18n row above). | T-20-05/ST-04 |
| deferred | soon | Web / UX | Surface condo-request (sindico) pending status for multi-provider owners | The legacy per-user status panels are now gated behind `ownsProviders === false`, so a caller who already owns ≥1 provider no longer sees their pending CONDO request status after submitting the sindico flow. Surface condo-request status on My Providers or a dedicated screen. | T-20-05/ST-04 |
| deferred | soon | Routing | Make My Providers the no-default landing instead of condo-setup | `requireDefaultProviderId` (`panel/provider/-resolve-active-provider.ts`) still redirects a no-default caller into `condo-setup`; the ST-01/02/03 known limitation. With My Providers now listing providers + launching the create flow, point the no-default landing at `/panel/provider/my-providers`. Deferred from ST-04 to keep the slice bounded (touches ST-01's resolver + the $providerId beforeLoad fallback). | T-20-05/ST-04 |


## Legacy backlog note

The full pre-cutover backlog corpus was archived to `.plan/archive/legacy-specify-memory/backlog.md` and `.plan/archive/legacy-specify-memory/deferred_backlog.md`. Use those files for historical retrieval during the backfill phase.
