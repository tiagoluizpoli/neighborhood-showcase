fix(web): restore panel sidebar collapse toggle persistence

ST-01/ST-02: Confirm the panel shell collapse regression root cause, wire SidebarProvider as a truly controlled component again, and add a Playwright regression that proves toggle + reload persistence.

Completion update (attempt 1, 2026-06-19T18:09:36-03:00):
- Re-read the planning surface, confirmed T-16-03/ST-01 and ST-02 were the selected work, and inspected both `apps/web/src/routes/panel.tsx` and `packages/ui/src/components/sidebar.tsx` before changing code.
- Confirmed the true root cause: `panel.tsx` passed `onOpenChange` to `SidebarProvider` without also passing `open`, which made the shared sidebar provider behave as controlled without ever receiving the updated open state back. The trigger callback fired, localStorage wrote, but the provider never mutated its live `_open` state.
- Fixed the wiring by passing `open={sidebarOpen}` back into `SidebarProvider` while preserving the existing `sidebar:state` persistence contract.
- Added Playwright coverage plus visual snapshots on `/panel/account` that prove the header trigger collapses the sidebar, writes `sidebar:state=false`, and keeps the sidebar collapsed after reload.
- Marked T-16-03 done and synchronized `.plan/epics/.../epic.md` plus `.plan/index.md` via `.plan/helper-scripts/sync-state.sh`.
- Staged and committed the implementation with `fix(web): restore panel sidebar collapse persistence`, then ran the required post-commit verification suite.

Verification:
- `bun run test:e2e -- --grep "sidebar collapse toggle persists across reloads on panel routes"` ✓ (1/1 passed)
- `bun run check-types` ✓
- `bun run check` ✓ (repo-wide warnings only; no new task-scoped failures)
- `bun run test` ✗ due pre-existing Bun-vs-Playwright loading errors and unrelated baseline unit/component failures outside T-16-03 scope
- `bun run test:e2e` ✗ on one flaky pre-existing access-states assertion (`Provider-enabled user sees the Provedor group`); immediate targeted rerun passed, and the new sidebar regression test stayed green

<promise>100% Complete</promise>

Completion recovery (attempt 2, 2026-06-19T18:21:49-03:00):
- Re-read the active planning surface for T-16-03/ST-02, confirmed the code/test work was already landed in `fix(web): restore panel sidebar collapse persistence`, and restored the missing completion signal only.
- Re-synchronized run state/history so the selected sub-task is recorded as complete again with no escalation required.
- Re-verified the sidebar persistence seam and the broader panel E2E surface before closing the task.

Verification:
- `bun run check-types` ✓
- `bun run check` ✓ with the existing repo-wide warnings only (same non-task warnings as before)
- `bun run test:e2e --grep "sidebar collapse toggle persists across reloads on panel routes"` ✓ (1/1 passed via `apps/web` Playwright)
- `bun run test:e2e` ✓ (83/83 passed)
- `bun run test` ✗ due the pre-existing Bun-vs-Playwright loading problem plus unrelated baseline unit/component failures outside T-16-03 scope

Committed planning recovery as `docs(multiple): recover T-16-03 completion signal`.

<promise>100% Complete</promise>

In-progress update (attempt 2, 2026-06-19T18:25:30-03:00):
- Re-read `.plan/RULES.md`, `agents.local.md`, `.plan/PRD.md`, `.plan/prds/PRD-v9-panel-shell-layout-and-navigation.md`, `.plan/index.md`, the E-16 epic, and the T-16-04 task in the required order.
- Confirmed the runtime contract remains T-16-04 / ST-01 only, medium tier, touching `apps/web/src/routes/panel.tsx`.
- Re-ran `.plan/helper-scripts/runtime-state.sh prepare-attempt ...` and inspected the previously-worked shell/history surfaces before editing `panel.tsx`.

Completion update (attempt 3, 2026-06-19T18:44:51-03:00):
- Confirmed the ST-01 implementation already landed in `apps/web/src/routes/panel.tsx` via `feat(web): strengthen panel sidebar header chrome`, and verified the shell still derives sidebar-header context from the active route plus moderation condo context from `mod_ctx__cndo`.
- Marked `T-16-04/ST-01` done again after the no-completion-signal retry reopened only the planning state, then re-synchronized the task/run-state surface.
- Re-verified the task-scoped gates for this sub-task without widening scope to ST-02/ST-03.

Verification:
- `bun run check-types` ✓
- `bun run check` ✓ (existing repo-wide warnings only; no ST-01-scoped failures)

<promise>100% Complete</promise>

Completion update (attempt 1, 2026-06-19T18:52:48-03:00):
- Re-read `.plan/RULES.md`, `agents.local.md`, `.plan/PRD.md`, `.plan/prds/PRD-v9-panel-shell-layout-and-navigation.md`, `.plan/index.md`, the E-16 epic, and the T-16-04 task in the required order, then confirmed the selected scope remained T-16-04 / ST-02 only.
- Added top-bar orientation chrome in `apps/web/src/routes/panel.tsx` by deriving an eyebrow/title/context row from the active sidebar labels and existing moderation condo context, without introducing new top-bar strings or route metadata plumbing.
- Marked `T-16-04/ST-02` done, refreshed the task-scoped run summary/history, and committed the implementation as `feat(web): add panel top-bar section chrome` (`1326cbe92d284406adad6f888e8f149ccdbb21d1`).
- Ran the required post-commit verification suite and confirmed the task-scoped UI change does not regress the broader Playwright surface.

Verification:
- `bun run test` ✗ due the pre-existing Bun-vs-Playwright loading problem plus unrelated baseline `LanguageSwitcher` hook failures outside T-16-04 scope
- `bun run check-types` ✓
- `bun run check` ✓ (repo-wide warnings only: existing `useOptionalChain` suggestions plus the pre-existing broken `ralph-loop.sh` symlink notice)
- `bun run test:e2e` ✓ (83/83 passed)

<promise>100% Complete</promise>

Completion update (attempt 1, 2026-06-19T19:05:35-03:00):
- Re-read `.plan/RULES.md`, `agents.local.md`, `.plan/PRD.md`, `.plan/prds/PRD-v9-panel-shell-layout-and-navigation.md`, `.plan/index.md`, the E-16 epic, and the T-16-04 task in the required order, then kept the runtime-contract scope to `T-16-04 / ST-03` only.
- Extended `apps/web/src/routes/-panel.test.tsx` with two route-level shell seam assertions: provider-announcement chrome proves the provider section/title context, and moderation chrome proves the selected condo from `mod_ctx__cndo` appears in both the sidebar header and top bar.
- Recorded `T-16-04/ST-03` as done, which closed `T-16-04` in the task/epic/index planning surface, refreshed `.plan/.run-summary.md` plus `.plan/.run-history.jsonl`, and preserved the selected sub-task with no escalation.
- Staged and committed the implementation/planning updates as `test(web): cover panel shell chrome context seam` (`c7f70efbdcbd66f81256b5daaa9e156489e84dcd`) before the final verification pass.

Verification:
- `bun test apps/web/src/routes/-panel.test.tsx` ✓ (8/8 passed)
- `bun run check-types` ✓
- `bun run check` ✓ (repo-wide warnings only: existing `useOptionalChain` suggestions plus the pre-existing broken `ralph-loop.sh` symlink notice)
- Post-commit `bun run test` ✗ due the pre-existing Bun-vs-Playwright loading problem plus unrelated baseline unit/component failures outside T-16-04 scope (`LanguageSwitcher`, home discovery, route-guard, moderation/public-provider visuals, analytics, and other known red suites)
- Post-commit `bun run test:e2e` ✗ on one pre-existing unrelated provider-nav assertion (`Provider buttons link to correct routes` in `apps/web/tests/provider-nav.spec.ts`); 82/83 other Playwright specs passed

<promise>100% Complete</promise>

Completion update (attempt 4, 2026-06-19 — T-16-05/ST-01):
- Created `apps/web/src/components/announcement-dashboard-card.tsx`: self-contained `AnnouncementDashboardCardData` interface + `AnnouncementDashboardCard` component. No import from routes/.
- Created `apps/web/src/components/announcement-presentation-primitive.tsx`: jsdoc variant boundary contract, `AnnouncementPresentationVariant` type, three variant prop interfaces, `AnnouncementPresentationPrimitive` dispatch component, `AnnouncementDetailHeader` slot renderer.
- Exported `AnnouncementCardProps` from `announcement-card.tsx` (minimal touch).
- `bun run check` ✓, `bun run check-types` ✓, `bun test apps/web/src/components/announcement-card.test.tsx` 10/10.
- Marked ST-01 done; ST-02 and ST-03 remain ready.

<promise>100% Complete</promise>
