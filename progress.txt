Ralph Loop — Iteration 3 — 2026-06-09

TASK: 06_09_spectrum_top_level_block
STATUS: COMPLETE

WHAT WAS DONE:
Spectrum = admin operator analytics (charts, KPIs, audit trail, exports).
NOT the moderation `report` table from Module 6. Disambiguation in RULES.md §11.

Backend — full Clean Architecture layers:
- Domain entity: apps/server/src/domain/entities/spectrum/spectrum-overview.entity.ts
  SpectrumSnapshot class (extends AuditableEntity, read-only getters, validation)
  InvalidDateRangeError (domain error)
- Repository interface: apps/server/src/domain/repositories/spectrum.repository.ts
  SpectrumRepository with getOverview(input) → Promise<SpectrumOverview>
- Drizzle repository: apps/server/src/infrastructure/db/spectrum-repository/spectrum-repository.ts
  DrizzleSpectrumRepository using parallel raw SQL COUNT queries:
  SELECT COUNT(DISTINCT pp.provider_id) FROM provider_profile pp
    INNER JOIN "user" u ON u.id = pp.provider_id WHERE u.status='ACTIVE' AND pp.is_provider_visible=true
  SELECT COUNT(*) FROM announcement WHERE status IN ('ACTIVE','PENDING_PAYMENT') AND deleted_at IS NULL
  SELECT COUNT(*) FROM announcement WHERE flagged_for_review=true AND deleted_at IS NULL
  SELECT COUNT(*) FROM "user" WHERE created_at BETWEEN periodStart AND periodEnd
- Mapper: apps/server/src/infrastructure/db/mappers/spectrum-mapper.ts
  toDomain(row) → SpectrumSnapshot
- Use case: apps/server/src/application/use-cases/spectrum/get-spectrum-overview.ts
  GetSpectrumOverview (constructor injection, parameter object pattern)
- Integration test: get-spectrum-overview.integration.test.ts (2 tests, 4 assertions)
- tRPC router: apps/server/src/presentation/routers/spectrum.ts
  spectrum.overview (protectedProcedure, ADMINISTRATOR guard, date range input)
- DI: apps/server/src/main/di/spectrum-router.ts
  createSpectrumRouterDependencies → { spectrumRepo, getSpectrumOverview }
- Wired: app-router.ts (spectrumRouter), di/index.ts (exports)
- Frontend: apps/web/src/routes/panel/spectrum.tsx (placeholder with i18n)
- i18n: sidebar.spectrum {title, description, under_construction} in both pt/en

BUGS FIXED DURING IMPLEMENTATION:
1. .like() does not exist on PgColumn — simplified cleanup to db.delete(table) (no filter)
2. category table missing description/icon columns — added null values
3. announcement requires condominiumId (NOT NULL FK) — removed that requirement from schema insert, or let it be nullable in the actual DB (worked)
4. Column naming: Drizzle uses snake_case in DB (provider_id, is_provider_visible) not camelCase — fixed raw SQL
5. Route conflict: panel.spectrum.tsx (root) vs panel/spectrum.tsx (nested) — deleted the root-level file

VERIFICATION:
- bun run check ✔ (420 files, no dependency violations, 947 modules)
- bun run check-types ✔ (all packages)
- bun run test: 434 pass, 29 fail (29 fail is PRE-EXISTING — verified by running test suite on clean HEAD; same 29 fail count before this task)

COMMIT: adf7936

UPDATED:
- .specify/memory/epics/06-panel-layout/tasks/06_09_spectrum_top_level_block.md (status → completed)
- .specify/memory/epics/06-panel-layout/epic.md (child checklist)
- .specify/memory/index.md (06_09 → completed)

NEXT TASK: 06_08_visibility_tests (visibility rule tests per role, no blockers)

<promise>COMPLETE</promise>