Ralph Loop iteration 8 — 2026-06-10

CURRENT TASK: Epic 13 Task 02 — ProviderProfile Backend (entity, repository, use cases)

## What was done

1. ProviderProfile entity (`apps/server/src/domain/entities/provider-profile.entity.ts`)
   - Extends AuditableEntity<ProviderProfileProps>
   - 9 read-only getters: displayName, avatarUrl, companyName, tradeName, logoUrl, bannerUrl, publicDescription, socialLinks, isProviderVisible
   - Constructor validates: displayName must trim to 3+ chars, publicDescription must be <=500 chars
   - Throws DomainError subclasses: InvalidProviderDisplayNameError, InvalidProviderPublicDescriptionError, ProviderProfileNotFoundError

2. ProviderProfileRepository interface (`apps/server/src/domain/repositories/provider-profile.repository.ts`)
   - findByProviderId(providerId): Promise<ProviderProfile | null>
   - upsert({ providerId, displayName, avatarUrl?, companyName?, tradeName?, logoUrl?, bannerUrl?, publicDescription?, socialLinks?, isProviderVisible? }): Promise<ProviderProfile>
   - delete(providerId): Promise<void>

3. GetProviderProfile use case (`apps/server/src/application/use-cases/provider-profile/get-provider-profile.ts`)
   - execute({ providerId }): throws ProviderProfileNotFoundError if repo returns null

4. UpdateProviderProfile use case (`apps/server/src/application/use-cases/provider-profile/update-provider-profile.ts`)
   - execute(input): trims displayName, rejects whitespace-only (length 0 after trim), rejects <3 chars, rejects publicDescription >500 chars
   - Calls repo.upsert with all fields

5. ProviderProfileMapper (`apps/server/src/infrastructure/db/mappers/provider-profile.mapper.ts`)
   - Implements EntityMapper<ProviderProfileSchemaSelect, ProviderProfile, ProviderProfileSchemaInsert>
   - toDomain(): maps DB row to ProviderProfile entity (uses raw.providerId as entity id)
   - toPersistence(): maps entity to insert row

6. ProviderProfileRepositoryImpl (`apps/server/src/infrastructure/db/provider-profile-repository.ts`)
   - findByProviderId: selects from provider_profile where providerId = input
   - upsert: insert with onConflictDoUpdate (target: providerId)
   - delete: delete where providerId = input

7. Unit tests: all 10 pass (get-provider-profile.test.ts: 2 pass, update-provider-profile.test.ts: 8 pass including whitespace-only displayName rejection)

8. Integration tests: created but fail on pre-existing test DB schema drift (user.language and user.theme columns in Drizzle schema but missing in test DB). Same root cause as all other existing integration tests (confirmed: get-user-profile.integration.test.ts also fails with "column 'language' does not exist"). Unit tests cover the same scenarios.

## Verification

check:arch ✔ | check-types ✔ | check (biome) ✔ | test-unit ✔ (10 pass, 0 fail)

Integration tests: 2 fail due to pre-existing test DB schema drift. NOT a task 02 issue.

## Status: COMPLETED

## Notes

- Clean Architecture boundaries strictly observed: domain has no Drizzle/tRPC, application has no Drizzle/tRPC, infrastructure has no tRPC
- Path depth issues fixed: repository impl uses `../../domain/` (not `../../../domain/`), integration tests use `../../../infrastructure/` (not `../../../../infrastructure/`)
- biome noNonNullAssertion rule: integration test uses `row as NonNullable<typeof row>` after null check to satisfy linter

## Next task

Epic 13 Task 03 — ProviderProfile Router (new trpc.providerProfile router with get and update procedures, wired through DI)